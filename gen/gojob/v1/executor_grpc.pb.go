// The executor contract.
//
// This file is the single source of truth for how a scheduler and an executor talk.
// It is generated into every executor's language, so a missing method or a wrong field
// type is a build failure rather than something discovered at registration time.
//
// Two services, in opposite directions:
//
//   JobExecutor  — hosted by the executor,  called by the scheduler
//   JobScheduler — hosted by the scheduler, called by the executor
//
// An executor implements JobExecutor completely. There is no partial implementation:
// generated stubs make every method mandatory, and section "Conformance" in doc/dispatch.md
// describes how that is enforced per language.

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             v7.35.1
// source: gojob/v1/executor.proto

package gojobv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	JobExecutor_Describe_FullMethodName     = "/gojob.v1.JobExecutor/Describe"
	JobExecutor_Run_FullMethodName          = "/gojob.v1.JobExecutor/Run"
	JobExecutor_Cancel_FullMethodName       = "/gojob.v1.JobExecutor/Cancel"
	JobExecutor_GetExecution_FullMethodName = "/gojob.v1.JobExecutor/GetExecution"
)

// JobExecutorClient is the client API for JobExecutor service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// ---------------------------------------------------------------------------
// Executor-hosted service. The scheduler calls these.
// ---------------------------------------------------------------------------
type JobExecutorClient interface {
	// Describe reports what this executor is and can do.
	//
	// The scheduler calls it during registration, before routing any work, and treats a
	// failure as a refused registration. It is also how contract-version compatibility is
	// established.
	Describe(ctx context.Context, in *DescribeRequest, opts ...grpc.CallOption) (*DescribeResponse, error)
	// Run hands over one execution.
	//
	// It returns immediately — the executor must NOT run the job before answering. An OK
	// response is a promise that a result will eventually be reported through
	// JobScheduler.ReportResult.
	//
	// A REFUSAL is `RunResponse.refused = true` with an OK status, never an error code. See
	// that field for why: a status cannot separate an application refusal from a transport
	// failure after delivery, and reading one as the other runs a job twice.
	//
	// Errors carry meaning and are part of the contract:
	//
	//	ALREADY_EXISTS      this (tenant, execution_key) is already held. The error's
	//	                    ExecutionHeld detail carries the run_token actually held, because
	//	                    "already held" is two different situations: a re-send of the SAME
	//	                    attempt after a timeout (adopt it), or a NEW attempt colliding
	//	                    with an older one the scheduler already fenced (do not adopt —
	//	                    the old handler is still running and this attempt never started)
	//
	// Anything else — RESOURCE_EXHAUSTED, UNAVAILABLE, FAILED_PRECONDITION, DEADLINE_EXCEEDED,
	// INTERNAL — is read by the scheduler as an UNKNOWN outcome, not as a refusal. Use the
	// field to refuse; use a status only when the call genuinely failed.
	Run(ctx context.Context, in *RunRequest, opts ...grpc.CallOption) (*RunResponse, error)
	// Cancel asks the executor to stop an execution.
	//
	// Acknowledgement means the stop was signalled, not that work has ceased. Cancellation
	// is cooperative; the executor still reports a result when the work actually ends.
	Cancel(ctx context.Context, in *CancelRequest, opts ...grpc.CallOption) (*CancelResponse, error)
	// GetExecution answers what happened to an execution the scheduler has lost track of.
	//
	// It is reliable for one question only: "are you running this right now?" A process that
	// has restarted has no memory of earlier work and answers NOT_FOUND, which means
	// UNKNOWN — never "it did not run". Durable proof that work happened belongs in the
	// handler's own idempotency key, not here.
	GetExecution(ctx context.Context, in *GetExecutionRequest, opts ...grpc.CallOption) (*GetExecutionResponse, error)
}

type jobExecutorClient struct {
	cc grpc.ClientConnInterface
}

func NewJobExecutorClient(cc grpc.ClientConnInterface) JobExecutorClient {
	return &jobExecutorClient{cc}
}

func (c *jobExecutorClient) Describe(ctx context.Context, in *DescribeRequest, opts ...grpc.CallOption) (*DescribeResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(DescribeResponse)
	err := c.cc.Invoke(ctx, JobExecutor_Describe_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *jobExecutorClient) Run(ctx context.Context, in *RunRequest, opts ...grpc.CallOption) (*RunResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RunResponse)
	err := c.cc.Invoke(ctx, JobExecutor_Run_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *jobExecutorClient) Cancel(ctx context.Context, in *CancelRequest, opts ...grpc.CallOption) (*CancelResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(CancelResponse)
	err := c.cc.Invoke(ctx, JobExecutor_Cancel_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *jobExecutorClient) GetExecution(ctx context.Context, in *GetExecutionRequest, opts ...grpc.CallOption) (*GetExecutionResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(GetExecutionResponse)
	err := c.cc.Invoke(ctx, JobExecutor_GetExecution_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// JobExecutorServer is the server API for JobExecutor service.
// All implementations should embed UnimplementedJobExecutorServer
// for forward compatibility.
//
// ---------------------------------------------------------------------------
// Executor-hosted service. The scheduler calls these.
// ---------------------------------------------------------------------------
type JobExecutorServer interface {
	// Describe reports what this executor is and can do.
	//
	// The scheduler calls it during registration, before routing any work, and treats a
	// failure as a refused registration. It is also how contract-version compatibility is
	// established.
	Describe(context.Context, *DescribeRequest) (*DescribeResponse, error)
	// Run hands over one execution.
	//
	// It returns immediately — the executor must NOT run the job before answering. An OK
	// response is a promise that a result will eventually be reported through
	// JobScheduler.ReportResult.
	//
	// A REFUSAL is `RunResponse.refused = true` with an OK status, never an error code. See
	// that field for why: a status cannot separate an application refusal from a transport
	// failure after delivery, and reading one as the other runs a job twice.
	//
	// Errors carry meaning and are part of the contract:
	//
	//	ALREADY_EXISTS      this (tenant, execution_key) is already held. The error's
	//	                    ExecutionHeld detail carries the run_token actually held, because
	//	                    "already held" is two different situations: a re-send of the SAME
	//	                    attempt after a timeout (adopt it), or a NEW attempt colliding
	//	                    with an older one the scheduler already fenced (do not adopt —
	//	                    the old handler is still running and this attempt never started)
	//
	// Anything else — RESOURCE_EXHAUSTED, UNAVAILABLE, FAILED_PRECONDITION, DEADLINE_EXCEEDED,
	// INTERNAL — is read by the scheduler as an UNKNOWN outcome, not as a refusal. Use the
	// field to refuse; use a status only when the call genuinely failed.
	Run(context.Context, *RunRequest) (*RunResponse, error)
	// Cancel asks the executor to stop an execution.
	//
	// Acknowledgement means the stop was signalled, not that work has ceased. Cancellation
	// is cooperative; the executor still reports a result when the work actually ends.
	Cancel(context.Context, *CancelRequest) (*CancelResponse, error)
	// GetExecution answers what happened to an execution the scheduler has lost track of.
	//
	// It is reliable for one question only: "are you running this right now?" A process that
	// has restarted has no memory of earlier work and answers NOT_FOUND, which means
	// UNKNOWN — never "it did not run". Durable proof that work happened belongs in the
	// handler's own idempotency key, not here.
	GetExecution(context.Context, *GetExecutionRequest) (*GetExecutionResponse, error)
}

// UnimplementedJobExecutorServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedJobExecutorServer struct{}

func (UnimplementedJobExecutorServer) Describe(context.Context, *DescribeRequest) (*DescribeResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Describe not implemented")
}
func (UnimplementedJobExecutorServer) Run(context.Context, *RunRequest) (*RunResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Run not implemented")
}
func (UnimplementedJobExecutorServer) Cancel(context.Context, *CancelRequest) (*CancelResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Cancel not implemented")
}
func (UnimplementedJobExecutorServer) GetExecution(context.Context, *GetExecutionRequest) (*GetExecutionResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method GetExecution not implemented")
}
func (UnimplementedJobExecutorServer) testEmbeddedByValue() {}

// UnsafeJobExecutorServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to JobExecutorServer will
// result in compilation errors.
type UnsafeJobExecutorServer interface {
	mustEmbedUnimplementedJobExecutorServer()
}

func RegisterJobExecutorServer(s grpc.ServiceRegistrar, srv JobExecutorServer) {
	// If the following call panics, it indicates UnimplementedJobExecutorServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&JobExecutor_ServiceDesc, srv)
}

func _JobExecutor_Describe_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(DescribeRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(JobExecutorServer).Describe(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: JobExecutor_Describe_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(JobExecutorServer).Describe(ctx, req.(*DescribeRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _JobExecutor_Run_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RunRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(JobExecutorServer).Run(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: JobExecutor_Run_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(JobExecutorServer).Run(ctx, req.(*RunRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _JobExecutor_Cancel_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(CancelRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(JobExecutorServer).Cancel(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: JobExecutor_Cancel_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(JobExecutorServer).Cancel(ctx, req.(*CancelRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _JobExecutor_GetExecution_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetExecutionRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(JobExecutorServer).GetExecution(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: JobExecutor_GetExecution_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(JobExecutorServer).GetExecution(ctx, req.(*GetExecutionRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// JobExecutor_ServiceDesc is the grpc.ServiceDesc for JobExecutor service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var JobExecutor_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "gojob.v1.JobExecutor",
	HandlerType: (*JobExecutorServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Describe",
			Handler:    _JobExecutor_Describe_Handler,
		},
		{
			MethodName: "Run",
			Handler:    _JobExecutor_Run_Handler,
		},
		{
			MethodName: "Cancel",
			Handler:    _JobExecutor_Cancel_Handler,
		},
		{
			MethodName: "GetExecution",
			Handler:    _JobExecutor_GetExecution_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "gojob/v1/executor.proto",
}

const (
	JobScheduler_Register_FullMethodName       = "/gojob.v1.JobScheduler/Register"
	JobScheduler_Heartbeat_FullMethodName      = "/gojob.v1.JobScheduler/Heartbeat"
	JobScheduler_ReportProgress_FullMethodName = "/gojob.v1.JobScheduler/ReportProgress"
	JobScheduler_ReportResult_FullMethodName   = "/gojob.v1.JobScheduler/ReportResult"
)

// JobSchedulerClient is the client API for JobScheduler service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// ---------------------------------------------------------------------------
// Scheduler-hosted service. Executors call these.
// ---------------------------------------------------------------------------
// Every call in both directions is authenticated at the transport: mutual TLS, or a
// pre-shared credential carried in metadata. This is not optional hardening.
//
// The dispatch endpoint executes work on request, so an unauthenticated JobExecutor is
// remote code execution by configuration. And an unauthenticated JobScheduler would let any
// process register itself for any tenant, declare a valuable handler, advertise capacity,
// and be handed that tenant's parameters and executions by ordinary round-robin routing —
// physical database isolation does not protect a dispatch path that trusts its callers.
//
// The scheduler binds the authenticated identity to what it may claim: an identity is
// authorized for a set of tenants and executor groups, and a Register naming anything
// outside that set is refused. Identity is never taken from the request body.
type JobSchedulerClient interface {
	// Register announces an executor and its handlers. The scheduler probes the executor
	// (Describe, plus a GetExecution for a key it cannot hold) before accepting.
	Register(ctx context.Context, in *RegisterRequest, opts ...grpc.CallOption) (*RegisterResponse, error)
	// Heartbeat keeps a registration alive. A response with known=false means the
	// registration lapsed and the executor must call Register again.
	Heartbeat(ctx context.Context, in *HeartbeatRequest, opts ...grpc.CallOption) (*HeartbeatResponse, error)
	// ReportProgress extends an execution's deadline and asks whether to continue.
	//
	// The executor's framework calls this on a timer while a handler runs. It must not
	// depend on the handler cooperating: the handlers most likely to run for hours are the
	// least able to check in.
	ReportProgress(ctx context.Context, in *ReportProgressRequest, opts ...grpc.CallOption) (*ReportProgressResponse, error)
	// ReportResult delivers a terminal outcome. Idempotent on (tenant, execution_key,
	// run_token): a redelivery of a result already recorded returns OK with
	// already_recorded=true, so a lost response never turns into a spurious ABORTED.
	//
	//	ABORTED   this attempt was fenced by a DIFFERENT token; discard and do not retry
	//	NOT_FOUND unknown execution; discard
	ReportResult(ctx context.Context, in *ReportResultRequest, opts ...grpc.CallOption) (*ReportResultResponse, error)
}

type jobSchedulerClient struct {
	cc grpc.ClientConnInterface
}

func NewJobSchedulerClient(cc grpc.ClientConnInterface) JobSchedulerClient {
	return &jobSchedulerClient{cc}
}

func (c *jobSchedulerClient) Register(ctx context.Context, in *RegisterRequest, opts ...grpc.CallOption) (*RegisterResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RegisterResponse)
	err := c.cc.Invoke(ctx, JobScheduler_Register_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *jobSchedulerClient) Heartbeat(ctx context.Context, in *HeartbeatRequest, opts ...grpc.CallOption) (*HeartbeatResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(HeartbeatResponse)
	err := c.cc.Invoke(ctx, JobScheduler_Heartbeat_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *jobSchedulerClient) ReportProgress(ctx context.Context, in *ReportProgressRequest, opts ...grpc.CallOption) (*ReportProgressResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ReportProgressResponse)
	err := c.cc.Invoke(ctx, JobScheduler_ReportProgress_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *jobSchedulerClient) ReportResult(ctx context.Context, in *ReportResultRequest, opts ...grpc.CallOption) (*ReportResultResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ReportResultResponse)
	err := c.cc.Invoke(ctx, JobScheduler_ReportResult_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// JobSchedulerServer is the server API for JobScheduler service.
// All implementations should embed UnimplementedJobSchedulerServer
// for forward compatibility.
//
// ---------------------------------------------------------------------------
// Scheduler-hosted service. Executors call these.
// ---------------------------------------------------------------------------
// Every call in both directions is authenticated at the transport: mutual TLS, or a
// pre-shared credential carried in metadata. This is not optional hardening.
//
// The dispatch endpoint executes work on request, so an unauthenticated JobExecutor is
// remote code execution by configuration. And an unauthenticated JobScheduler would let any
// process register itself for any tenant, declare a valuable handler, advertise capacity,
// and be handed that tenant's parameters and executions by ordinary round-robin routing —
// physical database isolation does not protect a dispatch path that trusts its callers.
//
// The scheduler binds the authenticated identity to what it may claim: an identity is
// authorized for a set of tenants and executor groups, and a Register naming anything
// outside that set is refused. Identity is never taken from the request body.
type JobSchedulerServer interface {
	// Register announces an executor and its handlers. The scheduler probes the executor
	// (Describe, plus a GetExecution for a key it cannot hold) before accepting.
	Register(context.Context, *RegisterRequest) (*RegisterResponse, error)
	// Heartbeat keeps a registration alive. A response with known=false means the
	// registration lapsed and the executor must call Register again.
	Heartbeat(context.Context, *HeartbeatRequest) (*HeartbeatResponse, error)
	// ReportProgress extends an execution's deadline and asks whether to continue.
	//
	// The executor's framework calls this on a timer while a handler runs. It must not
	// depend on the handler cooperating: the handlers most likely to run for hours are the
	// least able to check in.
	ReportProgress(context.Context, *ReportProgressRequest) (*ReportProgressResponse, error)
	// ReportResult delivers a terminal outcome. Idempotent on (tenant, execution_key,
	// run_token): a redelivery of a result already recorded returns OK with
	// already_recorded=true, so a lost response never turns into a spurious ABORTED.
	//
	//	ABORTED   this attempt was fenced by a DIFFERENT token; discard and do not retry
	//	NOT_FOUND unknown execution; discard
	ReportResult(context.Context, *ReportResultRequest) (*ReportResultResponse, error)
}

// UnimplementedJobSchedulerServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedJobSchedulerServer struct{}

func (UnimplementedJobSchedulerServer) Register(context.Context, *RegisterRequest) (*RegisterResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Register not implemented")
}
func (UnimplementedJobSchedulerServer) Heartbeat(context.Context, *HeartbeatRequest) (*HeartbeatResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Heartbeat not implemented")
}
func (UnimplementedJobSchedulerServer) ReportProgress(context.Context, *ReportProgressRequest) (*ReportProgressResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ReportProgress not implemented")
}
func (UnimplementedJobSchedulerServer) ReportResult(context.Context, *ReportResultRequest) (*ReportResultResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ReportResult not implemented")
}
func (UnimplementedJobSchedulerServer) testEmbeddedByValue() {}

// UnsafeJobSchedulerServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to JobSchedulerServer will
// result in compilation errors.
type UnsafeJobSchedulerServer interface {
	mustEmbedUnimplementedJobSchedulerServer()
}

func RegisterJobSchedulerServer(s grpc.ServiceRegistrar, srv JobSchedulerServer) {
	// If the following call panics, it indicates UnimplementedJobSchedulerServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&JobScheduler_ServiceDesc, srv)
}

func _JobScheduler_Register_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RegisterRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(JobSchedulerServer).Register(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: JobScheduler_Register_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(JobSchedulerServer).Register(ctx, req.(*RegisterRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _JobScheduler_Heartbeat_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(HeartbeatRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(JobSchedulerServer).Heartbeat(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: JobScheduler_Heartbeat_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(JobSchedulerServer).Heartbeat(ctx, req.(*HeartbeatRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _JobScheduler_ReportProgress_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ReportProgressRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(JobSchedulerServer).ReportProgress(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: JobScheduler_ReportProgress_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(JobSchedulerServer).ReportProgress(ctx, req.(*ReportProgressRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _JobScheduler_ReportResult_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ReportResultRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(JobSchedulerServer).ReportResult(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: JobScheduler_ReportResult_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(JobSchedulerServer).ReportResult(ctx, req.(*ReportResultRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// JobScheduler_ServiceDesc is the grpc.ServiceDesc for JobScheduler service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var JobScheduler_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "gojob.v1.JobScheduler",
	HandlerType: (*JobSchedulerServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Register",
			Handler:    _JobScheduler_Register_Handler,
		},
		{
			MethodName: "Heartbeat",
			Handler:    _JobScheduler_Heartbeat_Handler,
		},
		{
			MethodName: "ReportProgress",
			Handler:    _JobScheduler_ReportProgress_Handler,
		},
		{
			MethodName: "ReportResult",
			Handler:    _JobScheduler_ReportResult_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "gojob/v1/executor.proto",
}
